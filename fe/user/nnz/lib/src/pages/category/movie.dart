import 'package:flutter/material.dart';

class moviePage extends StatelessWidget {
  const moviePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("movie 페이지"),
      ),
    );
  }
}
