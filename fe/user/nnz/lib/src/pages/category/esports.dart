import 'package:flutter/material.dart';

class esportsPage extends StatelessWidget {
  const esportsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("e스포츠 페이지"),
      ),
    );
  }
}
