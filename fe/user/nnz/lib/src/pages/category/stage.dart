import 'package:flutter/material.dart';

class stagePage extends StatelessWidget {
  const stagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("stage 페이지"),
      ),
    );
  }
}
