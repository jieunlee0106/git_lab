import 'package:flutter/material.dart';

class sportsPage extends StatelessWidget {
  const sportsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("sports 페이지"),
      ),
    );
  }
}
