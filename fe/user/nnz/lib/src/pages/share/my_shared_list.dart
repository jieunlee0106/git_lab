import 'package:flutter/material.dart';

class MySharedList extends StatelessWidget {
  const MySharedList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("내가 참여한 나눔 페이지"),
      ),
    );
  }
}
