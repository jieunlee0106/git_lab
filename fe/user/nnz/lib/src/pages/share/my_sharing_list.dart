import 'package:flutter/material.dart';

class MySharingList extends StatelessWidget {
  const MySharingList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("내가 나눔 한 페이지"),
      ),
    );
  }
}
